<h1>New user</h1>

<% form_for(@user) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>
  <div id="foo_wrapper">
<%=render :partial => 'foo'%>
  </div>
<%= link_to 'Back', users_path %>