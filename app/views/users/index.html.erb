<h1>Listing users</h1>

<table>
  <tr>
  </tr>
<%  Renderer.first.find_mapping(:html) do |r| %>
  <% @users.each do |user| %>
  <%= r.run user %>
  <% end %>
<% end %>

</table>

<br />

<%= link_to 'New user', new_user_path %>